# 智慧郵件防護系統：專案進度

更新日期：2026-09-20

## 現在做到哪裡

後端原型與 Web UI 已串起 Google OAuth、Gmail 掃描、本機 SLM 分析、風險政策及 Gmail 標籤。模型架構已收斂成單一 SLM，不再執行 Hybrid 或模型加權。

目前仍是單機 Hackathon Demo。Android App、正式帳號系統及部署環境都還沒做。

## 本次重構

- 預設 backend 改為 `slm`，不再依 API Key 自動切換模型。
- 移除 `HybridEmailAnalyzer`、Hybrid 測試與 Hybrid requirements。
- SLM 改用二元 phishing classifier。
- phishing probability 直接轉為 0–100 risk score。
- winner probability 作為 confidence。
- confidence 低於 0.75 時改成獨立的 `uncertain` 決策。
- Gmail 加入 `AI-不確定` 標籤。
- 移除自動移至 Gmail 垃圾桶的行為。
- 長郵件改成多段分析，保留郵件最後一段。
- SLM 輸入採本文優先，再附加主旨、URL 與附件名稱；技術標頭保留在正規化資料中。

## 決策規則

```text
risk_score = phishing probability × 100
confidence = max(phishing probability, safe probability)
```

| 條件 | 決策 | Gmail 標籤 |
|---|---|---|
| confidence < 0.75 | 不確定 | `AI-不確定` |
| score 0–29 | 低度 | 無 |
| score 30–59 | 中度 | `AI-中度風險` |
| score 60–100 | 高度 | `AI-高度風險` |

任何分數都不會觸發自動垃圾桶動作。

## 預設模型

```text
songhieng/chn-roberta-phishing-content-detector-1.0
```

本機測試確認它能在 Ryzen 7 PRO 5850U 上以約 50–112 ms 處理一封短郵件，記憶體峰值約 711 MiB。但模型沒有公開訓練資料或 label mapping，且對中英混合內容有漏判。

目前暫定：

```env
SLM_PHISHING_LABEL=LABEL_1
SLM_MIN_CONFIDENCE=0.75
```

完整測試紀錄：`docs/model-smoke-test-chn-roberta.md`。

## 已完成

- FastAPI 後端與繁體中文 OpenAPI。
- Gmail API 連接器及 `NormalizedEmail` 格式。
- SPF、DKIM、DMARC 與必要技術標頭擷取。
- Google OAuth、Token 更新及 Windows DPAPI 持久化。
- 手動 Gmail 掃描與背景 polling。
- SLM 分段推論及 confidence gate。
- 低度、不確定、中度、高度四種 UI 分組。
- Gmail 風險標籤；不做自動刪除或垃圾桶移動。

## 尚未解決

### 模型可信度

預設模型在人工 smoke test 中出現正常通知誤判，也漏掉中英混合 phishing。Softmax 常接近 1，但不代表它已經校準。現階段只能作 Demo 的內容訊號。

### Label mapping

模型只提供 `LABEL_0` 與 `LABEL_1`。`LABEL_1=phishing` 是依控制案例推定，並非模型作者文件。程式已改成可透過環境變數調整。

### HTML 郵件

Gmail connector 主要讀取 `text/plain`。HTML-only 郵件會退回 snippet，可能漏掉真正連結與隱藏文字。

### 執行平台

Token 持久化依賴 Windows DPAPI。Linux、容器或多人部署需要另一套 token vault。

## 下一步

1. 完成重構後的單元測試與實機 SLM smoke test。
2. 在 UI 清楚顯示 risk score、confidence 及 uncertain 原因。
3. 補 HTML 郵件解析與 URL 正規化。
4. 將 URL、Reply-To 及 DMARC 的確定性規則與模型訊號分開顯示。
5. 準備固定的 Demo 郵件集，避免每次展示結果不同。
6. Hackathon 後再用正式中英文資料重新選模或 fine-tune。

測試期間只使用專用 Gmail。Client Secret、Access Token、Refresh Token 及 API Key 都不能寫入 Git 或 App。
