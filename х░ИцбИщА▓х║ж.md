# 智慧郵件防護系統：專案進度

更新日期：2026-09-18

## 專案目標

建立 Android／Gmail 智慧郵件防護系統，將「電子郵件連接」與「AI 風險判斷」分成獨立模組。

## 已完成

- 建立 Python FastAPI 後端。
- 建立 Gmail API 連接器。
- 建立 OpenAI 郵件分析器。
- 沒有 OpenAI API Key 時，可使用本機規則分析器測試。
- 建立 Google OAuth 登入、回呼及 Token 更新流程。
- Google OAuth Token 暫存在後端記憶體，程式重啟後需重新登入。
- 建立 Gmail 風險標籤與可復原的隔離方式。
- 完成四級風險政策與自動化測試。

## 風險政策

| 分數 | 層級 | 顯示與處理 |
|---:|---|---|
| 0–29 | 低度風險 | 正常顯示，不標顏色 |
| 30–59 | 中度風險 | 黃色提醒 |
| 60–84 | 高度風險 | 紅色提醒，停用連結與附件 |
| 85–100 | 明確惡意 | 加上 `AI-隔離` 標籤並移出收件匣 |

系統不會永久刪除郵件，以免誤判後無法復原。

## Google Cloud 設定

- 已建立 Google Cloud 專案與 OAuth 同意畫面。
- 使用者類型：外部。
- 發布狀態：測試。
- 應將專用測試 Gmail 加入「測試使用者」。
- 已加入 Scope：`https://www.googleapis.com/auth/gmail.modify`。
- OAuth 用戶端類型：網頁應用程式。
- Redirect URI：`http://127.0.0.1:8000/auth/google/callback`。
- Client ID 與 Client Secret 只能保存在 `.env`，不得上傳或貼到聊天。

## 本機設定

`.env` 應包含下列欄位，實際密碼不記錄在此文件：

```env
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://127.0.0.1:8000/auth/google/callback
```

## 啟動方式

### 使用 Visual Studio Code

以 VS Code 開啟 `C:\黑客松` 資料夾，按 `F5`，並選擇「啟動智慧郵件防護系統」。

### 使用 PowerShell

在 PowerShell 執行：

```powershell
cd C:\黑客松
.\.venv\Scripts\python.exe -m uvicorn mail_guard.api:app --host 127.0.0.1 --port 8000
```

保持 PowerShell 視窗開啟。

## 測試網址

- 健康檢查：`http://127.0.0.1:8000/health`
- Google 登入：`http://127.0.0.1:8000/auth/google/start`
- API 文件：`http://127.0.0.1:8000/docs`

登入成功後，在 API 文件執行：

```text
POST /api/gmail/scan-connected
```

測試內容：

```json
{
  "max_results": 3
}
```

## 驗證完成標準

1. `/health` 顯示 `google_oauth: configured`。
2. Google 登入後顯示「Gmail 連接成功」。
3. `/api/gmail/scan-connected` 成功回傳郵件分析結果。
4. Gmail 出現對應的 AI 風險標籤。

## 尚未完成

- 設定 OpenAI API Key，啟用真正的 GPT 分析。
- 建立 Android App UI。
- Android 端登入與後端 API 串接。
- 將 Google Token 改為加密持久保存。
- 加入正式的使用者登入與權限控制。
- 正式公開前完成 Google OAuth 驗證及必要安全評估。

## 安全提醒

- 不要在聊天或截圖中顯示 Client Secret、Access Token、Refresh Token 或 OpenAI API Key。
- 若秘密曾公開，必須立即重設或刪除相關憑證。
- 建議使用專門的測試 Gmail，不要先使用主要信箱。
- 測試期間每次只掃描少量郵件。
